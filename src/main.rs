mod encrypt;
mod errors;
mod files;
mod commands;
mod input;

use files::flags::*;
use files::locker::*;
use files::*;
use std::fs::OpenOptions;
use std::io::{Read, Seek, SeekFrom, Write};

fn main() {
    
}
